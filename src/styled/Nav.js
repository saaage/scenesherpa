import styled from 'styled-components'

const Nav = styled.nav`
  background: white;
  min-height: 2em;
  position: sticky;
  top: 0;
  width: 100vw;
  box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
  form {
    padding-top: 1em;
    padding-bottom: .5em;
  }
`

export default Nav
